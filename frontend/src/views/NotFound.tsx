import { NavHeader, PageContent, SideNav } from '@components';

export const NotFound = () => (
  <>
    <NavHeader title="Not Found" />
    <PageContent>
      <SideNav />
    </PageContent>
  </>
);
