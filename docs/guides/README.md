# Guides

Walkthroughs that start from a goal rather than an API. Each one is
self-contained; read the one you need.

| | |
| --- | --- |
| [In-game wire integration](wires.md) | reacting to in-game circuits from a plugin, and driving them back |
| [HTTPS with a real certificate](https.md) | a certificate browsers trust for the web UI, through Caddy or nginx |
| [Running on another machine](remote.md) | SSH, file sharing, and the web UI of an omegga that lives on a VPS |

[Pterodactyl](pterodactyl.md) is a guide too, but it is a way to install omegga
rather than something to do with one already running, so it sits under
[Setting up](../install/) alongside the other install paths.
